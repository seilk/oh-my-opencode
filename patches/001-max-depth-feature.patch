diff --git a/src/config/schema/background-task.ts b/src/config/schema/background-task.ts
index b955de6..0b17dc6 100644
--- a/src/config/schema/background-task.ts
+++ b/src/config/schema/background-task.ts
@@ -6,6 +6,7 @@ export const BackgroundTaskConfigSchema = z.object({
   modelConcurrency: z.record(z.string(), z.number().min(0)).optional(),
   /** Stale timeout in milliseconds - interrupt tasks with no activity for this duration (default: 180000 = 3 minutes, minimum: 60000 = 1 minute) */
   staleTimeoutMs: z.number().min(60000).optional(),
+  max_depth: z.number().min(0).optional(),
   /** Timeout for tasks that never received any progress update, falling back to startedAt (default: 600000 = 10 minutes, minimum: 60000 = 1 minute) */
   messageStalenessTimeoutMs: z.number().min(60000).optional(),
   syncPollTimeoutMs: z.number().min(60000).optional(),
diff --git a/src/features/background-agent/manager.ts b/src/features/background-agent/manager.ts
index 9e2baa1..109d7fb 100644
--- a/src/features/background-agent/manager.ts
+++ b/src/features/background-agent/manager.ts
@@ -151,7 +151,9 @@ export class BackgroundManager {
       throw new Error("Agent parameter is required")
     }
 
-    // Create task immediately with status="pending"
+    const parentTask = input.parentSessionID ? this.findBySession(input.parentSessionID) : undefined
+    const currentDepth = input.parentSessionID ? (parentTask?.depth ?? 0) + 1 : 0
+
     const task: BackgroundTask = {
       id: `bg_${crypto.randomUUID().slice(0, 8)}`,
       status: "pending",
@@ -170,6 +172,7 @@ export class BackgroundManager {
       fallbackChain: input.fallbackChain,
       attemptCount: 0,
       category: input.category,
+      depth: currentDepth,
     }
 
     this.tasks.set(task.id, task)
@@ -358,7 +361,7 @@ export class BackgroundManager {
         tools: (() => {
           const tools = {
             task: false,
-            call_omo_agent: true,
+            call_omo_agent: (task.depth ?? 0) < (this.config?.max_depth ?? 2),
             question: false,
             ...getAgentToolRestrictions(input.agent),
           }
@@ -494,7 +497,9 @@ export class BackgroundManager {
 
     const concurrencyGroup = input.concurrencyKey ?? input.agent ?? "task"
 
-    // Acquire concurrency slot if a key is provided
+    const parentTask = input.parentSessionID ? this.findBySession(input.parentSessionID) : undefined
+    const currentDepth = input.parentSessionID ? (parentTask?.depth ?? 0) + 1 : 0
+
     if (input.concurrencyKey) {
       await this.concurrencyManager.acquire(input.concurrencyKey)
     }
@@ -516,6 +521,7 @@ export class BackgroundManager {
       parentAgent: input.parentAgent,
       concurrencyKey: input.concurrencyKey,
       concurrencyGroup,
+      depth: currentDepth,
     }
 
     this.tasks.set(task.id, task)
@@ -569,6 +575,10 @@ export class BackgroundManager {
     existingTask.completedAt = undefined
     existingTask.error = undefined
     existingTask.parentSessionID = input.parentSessionID
+
+    const resumeParentTask = input.parentSessionID ? this.findBySession(input.parentSessionID) : undefined
+    existingTask.depth = input.parentSessionID ? (resumeParentTask?.depth ?? 0) + 1 : 0
+
     existingTask.parentMessageID = input.parentMessageID
     existingTask.parentModel = input.parentModel
     existingTask.parentAgent = input.parentAgent
@@ -631,7 +641,7 @@ export class BackgroundManager {
         tools: (() => {
           const tools = {
             task: false,
-            call_omo_agent: true,
+            call_omo_agent: (existingTask.depth ?? 0) < (this.config?.max_depth ?? 2),
             question: false,
             ...getAgentToolRestrictions(existingTask.agent),
           }
diff --git a/src/features/background-agent/types.ts b/src/features/background-agent/types.ts
index 6973dd7..c3404fe 100644
--- a/src/features/background-agent/types.ts
+++ b/src/features/background-agent/types.ts
@@ -54,6 +54,8 @@ export interface BackgroundTask {
   lastMsgCount?: number
   /** Number of consecutive polls with stable message count */
   stablePolls?: number
+
+  depth?: number
 }
 
 export interface LaunchInput {
